name: Deploy TaskFlow

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  security-gate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        dir: [backend, frontend]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Semgrep (errors only)
        run: |
          docker run --rm -v "$PWD:/src" -w /src semgrep/semgrep \
            semgrep ci --severity ERROR
        env:
          SEMGREP_RULES: p/default p/javascript p/nodejs p/owasp-top-ten p/secrets
      - name: Trivy filesystem scan (${{ matrix.dir }})
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: fs
          scan-ref: ${{ matrix.dir }}
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: '1'
      - name: Build ${{ matrix.dir }} image
        run: docker build -t taskflow-${{ matrix.dir }}:gate ./${{ matrix.dir }}
      - name: Trivy image scan (${{ matrix.dir }})
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: image
          image-ref: taskflow-${{ matrix.dir }}:gate
          severity: HIGH,CRITICAL
          exit-code: '1'

  deploy:
    needs: security-gate
    runs-on: ubuntu-latest
    steps:
      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT }}
          script: |
            set -e
            cd ~/taskflow
            git fetch origin main
            git reset --hard origin/main
            docker compose up -d --build --remove-orphans
            docker image prune -f
            echo "Deploy complete at $(date)"
